# 🧪 تست اتصال VPN

## آنچه تغییر کرد:

✅ دستور اجرای v2ray از:
```
v2ray.exe -config config.json
```

به:
```
v2ray.exe run -config config.json
```

✅ لاگ‌های بیشتر اضافه شد
✅ Timeout برای startup افزایش یافت (3 ثانیه)

---

## 🔍 تست کنید:

### 1. برنامه را دوباره باز کنید

```bash
cd G:\vpn\desktop-app
npm start
```

### 2. روی سرور کلیک کنید و "اتصال"

### 3. DevTools را باز کنید (Ctrl+Shift+I)

در Console باید ببینید:
```
=== شروع اتصال VPN ===
✓ v2ray-core موجود است
✓ کانفیگ پارس شد: {server: "...", port: ...}
✓ فایل کانفیگ ایجاد شد
در حال اجرای v2ray-core...
V2Ray stdout: V2Ray 5.19.0 started
✓ v2ray-core در حال اجرا است
✓ System Proxy فعال شد
=== اتصال موفق ===
```

### 4. بررسی v2ray process:

```bash
tasklist | findstr v2ray
```

باید ببینید:
```
v2ray.exe                    12345 Console                    1     25,432 K
```

### 5. بررسی Proxy:

```bash
reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings" /v ProxyEnable
```

باید ببینید:
```
ProxyEnable    REG_DWORD    0x1
```

### 6. تست IP:

```
https://ipinfo.io
```

باید IP سرور VPN را نشان دهد!

---

## 🐛 اگر باز مشکل داشت:

### اجرای دستی v2ray:

```bash
cd G:\vpn\desktop-app
.\v2ray-core\v2ray.exe run -config v2ray-config.json
```

اگر خطا داد، خطا را بگویید تا رفع کنیم.

---

## ⚡ نکات:

- اولین اتصال 3-5 ثانیه طول می‌کشد
- v2ray در پس‌زمینه اجرا می‌شود
- لاگ‌ها در DevTools نمایش داده می‌شوند
- اگر مشکلی بود، در Console نمایش داده می‌شود

---

موفق باشید! 🚀

