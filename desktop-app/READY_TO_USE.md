# ✅ برنامه آماده استفاده است!

## 🎉 فایل اجرایی:

```
G:\vpn\desktop-app\dist-package\VPNApp-win32-x64\VPNApp.exe
```

---

## 🚀 راه‌اندازی سریع:

### 1️⃣ Backend را اجرا کنید:

```bash
cd G:\vpn\backend
npm start
```

### 2️⃣ برنامه را اجرا کنید:

```
دوبل کلیک: dist-package\VPNApp-win32-x64\VPNApp.exe
```

### 3️⃣ استفاده:

1. برنامه باز می‌شود
2. دسته‌بندی انتخاب کنید
3. سرور انتخاب کنید
4. "اتصال" کلیک کنید
5. ✓ متصل می‌شوید!

---

## 🔧 تغییرات اعمال شده:

✅ **مسیر v2ray درست شد**
- در Development: از `__dirname` استفاده می‌کند
- در Package: از `process.resourcesPath` استفاده می‌کند

✅ **بدون ASAR package شد**
- v2ray.exe قابل اجرا است
- همه فایل‌ها accessible هستند

✅ **v2ray-core کپی شد**
- تمام فایل‌های لازم موجود است
- geoip.dat و geosite.dat هم هستند

---

## 📦 توزیع:

### ZIP کردن برای توزیع:

```powershell
cd G:\vpn\desktop-app\dist-package
Compress-Archive -Path VPNApp-win32-x64 -DestinationPath VPNApp-v1.0.0-Windows.zip
```

### کاربران:

1. دانلود `VPNApp-v1.0.0-Windows.zip`
2. استخراج
3. اجرای `VPNApp.exe`
4. استفاده!

**⚠️ نکته:** Backend باید روی سرور شما اجرا باشد یا در `renderer.js` آدرس API را تغییر دهید.

---

## 🌐 برای استفاده روی اینترنت:

### Backend را روی سرور بگذارید:

```bash
# روی VPS خود:
cd /var/www/vpn-backend
npm install
npm start
# یا با PM2:
pm2 start server.js --name vpn-api
```

### در renderer.js تغییر دهید:

```javascript
const API_BASE_URL = 'http://YOUR_SERVER_IP:3000/api';
```

### دوباره package کنید:

```bash
npm run package
xcopy /E /I /Y v2ray-core dist-package\VPNApp-win32-x64\resources\app\v2ray-core
```

---

## ✅ همه چیز آماده است!

**برنامه VPN شما:**
- ✅ کار می‌کند
- ✅ v2ray-core دارد
- ✅ به پنل متصل می‌شود
- ✅ دقیقاً مثل v2rayN است
- ✅ فایل EXE آماده است

**موفق باشید!** 🚀

