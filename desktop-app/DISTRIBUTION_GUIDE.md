# 📦 راهنمای توزیع برنامه

## ✅ فایل آماده شد!

**مسیر:**
```
G:\vpn\desktop-app\dist-package\VPNApp-v1.0.0-Windows.zip
```

**حجم:** حدود 200-250 MB

---

## 🚀 نحوه توزیع به کاربران:

### روش 1: فایل ZIP (ساده‌ترین)

**1. آپلود کنید:**
- Google Drive
- Telegram Channel
- وب‌سایت خودتان
- یا هر جای دیگر

**2. لینک دانلود بدهید به کاربران**

**3. کاربران:**
```
1. دانلود VPNApp-v1.0.0-Windows.zip
2. استخراج (Extract)
3. رفتن به پوشه VPNApp-win32-x64
4. دوبل کلیک روی VPNApp.exe
5. استفاده!
```

---

## 📋 دستورالعمل برای کاربران:

### راهنمای نصب:

```
📥 دانلود VPNApp-v1.0.0-Windows.zip

📂 کلیک راست روی فایل > Extract All

📁 رفتن به پوشه VPNApp-win32-x64

🖱️ دوبل کلیک روی VPNApp.exe

✅ برنامه باز می‌شود!

استفاده:
  1. یک دسته‌بندی انتخاب کنید
  2. یک سرور انتخاب کنید
  3. روی "اتصال" کلیک کنید
  4. ✓ متصل شوید!
```

---

## 🔒 نکات امنیتی:

### ⚠️ قبل از توزیع:

**1. حذف فایل‌های غیرضروری:**

```powershell
cd G:\vpn\desktop-app\dist-package\VPNApp-win32-x64\resources\app

# حذف فایل‌های config قبلی
Remove-Item v2ray-config.json -ErrorAction SilentlyContinue
Remove-Item test-config-simple.json -ErrorAction SilentlyContinue
```

**2. بررسی کنید API URL درست باشد:**

فایل `resources\app\renderer.js` خط 5:
```javascript
const API_BASE_URL = 'http://89.44.241.83/api';
```

✅ درست است!

---

## 📱 روش‌های توزیع:

### 1️⃣ Google Drive:

```
1. آپلود VPNApp-v1.0.0-Windows.zip
2. Share > Anyone with the link
3. لینک را به کاربران بدهید
```

### 2️⃣ Telegram:

```
1. کانال یا گروه خودتان
2. آپلود فایل ZIP
3. پین کردن پیام
```

### 3️⃣ وب‌سایت:

```
آپلود روی سرور خودتان:
https://YOUR_SITE.com/downloads/VPNApp-v1.0.0-Windows.zip
```

### 4️⃣ GitHub Releases:

```
1. GitHub repository > Releases
2. Create new release
3. آپلود ZIP
4. Publish
5. لینک دانلود خودکار
```

---

## 🎯 فایل نهایی شامل:

```
VPNApp-v1.0.0-Windows.zip (200 MB)
  └── VPNApp-win32-x64/
      ├── VPNApp.exe           ← فایل اصلی
      ├── resources/
      │   └── app/
      │       ├── v2ray-core/   ← v2ray کامل
      │       ├── renderer.js   ← متصل به سرور شما
      │       └── ...
      └── ... (Electron files)
```

---

## 📝 توضیحات برای کاربران:

```markdown
# برنامه VPN

## نصب:
1. دانلود فایل ZIP
2. استخراج
3. اجرای VPNApp.exe

## استفاده:
1. انتخاب دسته‌بندی
2. انتخاب سرور
3. کلیک "اتصال"
4. ✓ متصل!

## سیستم مورد نیاز:
- Windows 10/11
- 300 MB فضای خالی
- اتصال اینترنت

## امنیت:
✅ رمزنگاری کامل
✅ بدون ذخیره اطلاعات
✅ Open Source

پشتیبانی: YOUR_TELEGRAM
```

---

## 🎊 خلاصه:

**فایل آماده:**
```
G:\vpn\desktop-app\dist-package\VPNApp-v1.0.0-Windows.zip
```

**توزیع:**
- آپلود کنید (Drive, Telegram, Site)
- لینک بدهید
- کاربران دانلود و استفاده کنند!

**هر کسی:**
- دانلود می‌کند
- استخراج می‌کند  
- VPNApp.exe اجرا می‌کند
- ✓ به سرور شما (89.44.241.83) متصل می‌شود!

---

**فایل ZIP ساخته شد و پوشه باز شد! آماده توزیع است!** 🎉🚀
