# 🖥️ VPN Desktop App - Windows

برنامه دسکتاپ VPN با Electron برای ویندوز

## ✨ ویژگی‌ها

- ✅ نمایش دسته‌بندی سرورها
- ✅ لیست سرورها بر اساس دسته‌بندی
- ✅ اتصال به سرورهای VPN  
- ✅ قطع اتصال VPN
- ✅ کپی کردن لینک کانفیگ
- ✅ رابط کاربری زیبا و فارسی
- ✅ اجرا روی ویندوز
- ✅ خروجی EXE قابل نصب

## 🚀 اجرا

### نصب Dependencies:

```bash
cd desktop-app
npm install
```

### اجرای برنامه:

```bash
npm start
```

## 📦 ساخت فایل نصبی (EXE)

```bash
npm run build:win
```

خروجی در: `desktop-app/dist/`

## 🔧 تنظیمات

### Backend URL:

در فایل `renderer.js`:

```javascript
const API_BASE_URL = 'http://localhost:3000/api';
```

برای سرور روی شبکه:
```javascript
const API_BASE_URL = 'http://YOUR_SERVER_IP:3000/api';
```

## 📱 استفاده

### 1. اطمینان از اجرای Backend

```bash
cd G:\vpn\backend
npm start
```

### 2. اجرای برنامه دسکتاپ

```bash
cd G:\vpn\desktop-app
npm start
```

### 3. کار با برنامه

1. برنامه باز می‌شود
2. لیست دسته‌بندی‌ها نمایش داده می‌شود
3. روی یک دسته‌بندی کلیک کنید
4. لیست سرورها نمایش داده می‌شود
5. روی "اتصال" کلیک کنید
6. متصل می‌شوید!

## 🎨 UI

- طراحی مدرن با gradient background
- رابط کاربری فارسی (RTL)
- انیمیشن‌های روان
- Responsive و کاربرپسند

## 🔌 ویژگی‌های فنی

- **Electron**: برای ساخت برنامه Native
- **Axios**: برای ارتباط با Backend
- **HTML/CSS/JavaScript**: برای UI
- **IPC Communication**: برای ارتباط بین Process ها

## 📁 ساختار پروژه

```
desktop-app/
├── main.js           # Main Process (Electron)
├── renderer.js       # Renderer Process (UI Logic)
├── index.html        # UI Layout
├── styles.css        # Styling
├── package.json      # Dependencies
└── assets/           # تصاویر و آیکون‌ها
```

## 🐛 عیب‌یابی

### برنامه باز نمی‌شود:

```bash
# حذف node_modules و نصب مجدد
rm -rf node_modules
npm install
npm start
```

### خطای اتصال به Backend:

- مطمئن شوید Backend در حال اجرا است
- پورت 3000 را چک کنید
- URL را در `renderer.js` بررسی کنید

### برنامه Crash می‌کند:

```bash
# مشاهده لاگ‌ها
npm start
# Console را در برنامه باز کنید (Ctrl+Shift+I)
```

## 📦 ساخت Installer

### NSIS Installer (پیشنهادی):

```bash
npm run build:win
```

خروجی: `dist/VPN App Setup 1.0.0.exe`

### Portable Version:

فایل EXE بدون نیاز به نصب در `dist/` ایجاد می‌شود.

## 🔐 اتصال VPN واقعی

⚠️ **نکته:** در حال حاضر اتصال VPN شبیه‌سازی شده است.

برای اتصال واقعی:
1. نصب کتابخانه VPN (مثل OpenVPN, WireGuard)
2. ایجاد Native Module
3. استفاده از Child Process برای اجرای VPN client

## 🎯 TODO

- [ ] پیاده‌سازی واقعی VPN
- [ ] نمایش سرعت اتصال
- [ ] تاریخچه اتصالات
- [ ] تنظیمات پیشرفته
- [ ] Auto-update
- [ ] Tray Icon

## 📄 لایسنس

MIT License

## 🙏 ساخته شده با

- Electron
- Love ❤️

---

موفق باشید! 🚀

