# 📘 راهنمای استفاده از برنامه VPN ویندوز

## 🎯 آنچه برنامه انجام می‌دهد:

✅ **اتصال به پنل مدیریت**
- به Backend متصل می‌شود (localhost:3000)
- دسته‌بندی‌ها و کانفیگ‌ها را از پنل می‌گیرد

✅ **نمایش دسته‌بندی‌ها**
- تمام دسته‌بندی‌های ایجاد شده در پنل را نشان می‌دهد
- هر دسته‌بندی یک کارت رنگی دارد

✅ **نمایش سرورها**
- با کلیک روی هر دسته‌بندی، سرورهای آن نشان داده می‌شوند
- نام سرور، لینک کانفیگ و دکمه اتصال

✅ **اتصال به VPN**
- با کلیک روی "اتصال"، به سرور متصل می‌شوید
- وضعیت اتصال در بالای صفحه نشان داده می‌شود
- رنگ پنل از آبی به سبز تغییر می‌کند

✅ **قطع اتصال**
- دکمه "قطع اتصال" برای disconnect

---

## 🚀 نحوه استفاده:

### مرحله 1: اطمینان از اجرای Backend

```bash
cd G:\vpn\backend
npm start
```

باید ببینید: `Server is running on port 3000`

### مرحله 2: اطمینان از وجود داده در پنل

پنل ادمین را باز کنید: http://localhost:3001

**ایجاد دسته‌بندی:**
1. روی "دسته‌بندی‌ها" کلیک کنید
2. "+ افزودن دسته‌بندی جدید"
3. نام: "ایرانسل"
4. توضیحات: "سرورهای مخصوص ایرانسل"
5. ذخیره

**ایجاد کانفیگ:**
1. روی "کانفیگ‌ها" کلیک کنید
2. "+ افزودن کانفیگ جدید"
3. نام: "سرور آلمان 1"
4. لینک کانفیگ:
```
vless://451785f9-1142-40fb-8943-d615ee3e3737@panel.bananaid.ir:45677?encryption=none&security=none&type=tcp&headerType=http#server1
```
5. دسته‌بندی: "ایرانسل"
6. فعال: ✓
7. ذخیره

### مرحله 3: اجرای برنامه دسکتاپ

```bash
cd G:\vpn\desktop-app
npm start
```

پنجره برنامه باز می‌شود!

### مرحله 4: کار با برنامه

#### 📋 صفحه اصلی:
- **بالای صفحه:** وضعیت اتصال (قرمز = غیرفعال، سبز = متصل)
- **پنل بزرگ:** اطلاعات اتصال فعلی
- **کارت‌های رنگی:** دسته‌بندی‌های موجود

#### 🖱️ انتخاب سرور:
1. روی یک دسته‌بندی کلیک کنید (مثلاً "ایرانسل")
2. لیست سرورهای آن دسته نمایش داده می‌شود
3. هر سرور شامل:
   - نام سرور
   - لینک کانفیگ
   - دکمه "اتصال"
   - دکمه "کپی لینک"

#### 🔌 اتصال به سرور:
1. روی دکمه "اتصال" کلیک کنید
2. برنامه در حال اتصال... (1 ثانیه)
3. **وضعیت تغییر می‌کند:**
   - نقطه بالای صفحه سبز می‌شود
   - متن "متصل" نشان داده می‌شود
   - پنل اتصال سبز می‌شود
   - نام سرور نشان داده می‌شود
4. Alert می‌آید: "به سرور متصل شدید"

#### ❌ قطع اتصال:
1. روی دکمه "قطع اتصال" کلیک کنید (بالا یا در کارت سرور)
2. Alert: "اتصال قطع شد"
3. وضعیت به حالت عادی برمی‌گردد

#### 📋 کپی لینک:
1. روی دکمه "کپی لینک" کلیک کنید
2. لینک کانفیگ کپی می‌شود
3. می‌توانید در v2rayNG یا سایر برنامه‌ها استفاده کنید

---

## 🎨 رابط کاربری:

### رنگ‌ها:
- **آبی-بنفش:** حالت عادی
- **سبز:** متصل
- **قرمز:** دکمه قطع اتصال

### انیمیشن‌ها:
- نقطه وضعیت پالس می‌زند
- کارت‌ها با hover بالا می‌آیند
- تغییر رنگ smooth

---

## ⚠️ نکته مهم - اتصال VPN واقعی:

**در حال حاضر:** اتصال VPN شبیه‌سازی شده است! یعنی:
- ✅ به Backend متصل می‌شود
- ✅ دسته‌بندی‌ها و سرورها را نشان می‌دهد
- ✅ UI تغییر می‌کند
- ❌ ترافیک واقعاً از VPN نمی‌گذرد

**برای اتصال واقعی VPN نیاز است:**

### راه 1: استفاده از v2ray-core

باید v2ray-core.exe را دانلود و با برنامه integrate کنید:

```javascript
// در renderer.js:
const { spawn } = require('child_process');
const path = require('path');

async function connectVPN(server) {
    // ذخیره کانفیگ در فایل
    const configFile = path.join(__dirname, 'config.json');
    const config = parseAndCreateV2RayConfig(server.config_url);
    fs.writeFileSync(configFile, JSON.stringify(config));
    
    // اجرای v2ray
    const v2rayProcess = spawn('v2ray-core.exe', ['-config', configFile]);
    
    v2rayProcess.stdout.on('data', (data) => {
        console.log(`V2Ray: ${data}`);
    });
    
    // تنظیم System Proxy
    setSystemProxy('127.0.0.1', 10808);
}
```

### راه 2: استفاده از WireGuard

اگر سرورهای WireGuard دارید:

```javascript
const { exec } = require('child_process');

function connectWireGuard(configFile) {
    exec(`wg-quick up ${configFile}`, (error, stdout, stderr) => {
        if (error) {
            console.error('Error:', error);
            return;
        }
        console.log('Connected!');
    });
}
```

### راه 3: تبدیل به v2rayN مثل برنامه

برای اینکه برنامه مثل v2rayNG کار کند:

1. **دانلود v2ray-core:**
```
https://github.com/v2fly/v2ray-core/releases
```

2. **قرار دادن در پوشه برنامه:**
```
desktop-app/
├── v2ray-core/
│   ├── v2ray.exe
│   └── ...
```

3. **تبدیل vless:// به JSON config:**
```javascript
function parseVlessToConfig(vlessUrl) {
    // پارس vless://
    // تبدیل به config.json
    return {
        "inbounds": [{
            "port": 10808,
            "protocol": "socks",
            "settings": {}
        }],
        "outbounds": [{
            "protocol": "vless",
            "settings": {
                "vnext": [{
                    "address": server,
                    "port": port,
                    "users": [{"id": uuid}]
                }]
            }
        }]
    };
}
```

4. **تنظیم System Proxy:**
```javascript
const { exec } = require('child_process');

function setSystemProxy(host, port) {
    // Windows Registry
    exec(`reg add "HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Internet Settings" /v ProxyEnable /t REG_DWORD /d 1 /f`);
    exec(`reg add "HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Internet Settings" /v ProxyServer /d "${host}:${port}" /f`);
}

function disableSystemProxy() {
    exec(`reg add "HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Internet Settings" /v ProxyEnable /t REG_DWORD /d 0 /f`);
}
```

---

## 📦 ساخت فایل نصبی:

```bash
npm run build:win
```

خروجی:
- `dist/VPN App Setup 1.0.0.exe` - Installer
- `dist/VPN App 1.0.0.exe` - Portable

---

## 🔧 تنظیمات پیشرفته:

### تغییر IP Backend:

در `renderer.js`:
```javascript
const API_BASE_URL = 'http://YOUR_SERVER_IP:3000/api';
```

### تغییر استایل:

فایل `styles.css` را ویرایش کنید.

### اضافه کردن ویژگی:

فایل `renderer.js` را ویرایش کنید.

---

## 📚 فایل‌های مهم:

- `main.js` - Electron Main Process
- `renderer.js` - UI Logic + API
- `index.html` - Layout
- `styles.css` - Styling
- `package.json` - Dependencies

---

## 🎯 خلاصه:

**الان:**
- ✅ رابط کاربری کامل
- ✅ اتصال به Backend
- ✅ نمایش دسته‌بندی‌ها و سرورها
- ✅ UI اتصال/قطع اتصال
- ⚠️ اتصال VPN شبیه‌سازی

**برای Production:**
- 📥 دانلود v2ray-core
- 🔧 Integrate با برنامه
- 🌐 تنظیم System Proxy
- ✅ تست اتصال واقعی

---

موفق باشید! 🚀

اگر سوالی دارید یا کمک نیاز دارید، بگویید! 😊

