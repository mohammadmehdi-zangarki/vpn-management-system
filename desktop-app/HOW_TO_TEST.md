# 🧪 راهنمای تست برنامه

## 📍 مسیر فایل اجرایی:

```
G:\vpn\desktop-app\dist-package\VPNApp-win32-x64\VPNApp.exe
```

---

## 🚀 مراحل تست:

### 1️⃣ اطمینان از اجرای Backend

**ترمینال 1:**
```bash
cd G:\vpn\backend
npm start
```

باید ببینید: `Server is running on port 3000`

### 2️⃣ اجرای برنامه

**دو روش:**

**روش 1: دوبل کلیک**
```
رفتن به: G:\vpn\desktop-app\dist-package\VPNApp-win32-x64\
دوبل کلیک: VPNApp.exe
```

**روش 2: از CMD/PowerShell**
```bash
cd G:\vpn\desktop-app\dist-package\VPNApp-win32-x64
.\VPNApp.exe
```

### 3️⃣ استفاده از برنامه

1. **پنجره برنامه باز می‌شود**
   - Header: وضعیت اتصال (نقطه قرمز/سبز)
   - پنل: اطلاعات اتصال
   - کارت‌ها: دسته‌بندی‌های موجود

2. **انتخاب دسته‌بندی**
   - روی کارت رنگی کلیک کنید
   - لیست سرورها نمایش داده می‌شود

3. **اتصال به سرور**
   - روی دکمه "اتصال" کلیک کنید
   - صبر کنید (2-3 ثانیه)
   - Alert: "متصل شدید!"

4. **تست IP**
   - مرورگر را ببندید و دوباره باز کنید
   - باز کنید: https://ipinfo.io
   - باید IP سرور VPN را ببینید!

5. **قطع اتصال**
   - روی "قطع اتصال" کلیک کنید
   - Proxy غیرفعال می‌شود
   - اینترنت عادی برمی‌گردد

---

## 🔍 Debug (اگر مشکل داشت):

### باز کردن DevTools:

در برنامه:
```
Ctrl + Shift + I
```

در Console می‌بینید:
```
V2Ray Manager initialized:
  isPackaged: false
  v2rayPath: ...
  configPath: ...
```

اگر خطا داد، لاگ‌ها را می‌بینید.

---

## ✅ تست موفق:

اگر همه چیز درست باشد:

1. ✅ برنامه باز می‌شود
2. ✅ دسته‌بندی‌ها نمایش داده می‌شوند
3. ✅ سرورها نمایش داده می‌شوند
4. ✅ اتصال برقرار می‌شود
5. ✅ v2ray.exe اجرا می‌شود (Task Manager)
6. ✅ Proxy فعال می‌شود (Settings > Network)
7. ✅ IP تغییر می‌کند (ipinfo.io)

---

## 🎯 تست سریع (30 ثانیه):

```bash
# 1. Backend
cd G:\vpn\backend
npm start

# 2. برنامه (در پنجره دیگر)
cd G:\vpn\desktop-app\dist-package\VPNApp-win32-x64
.\VPNApp.exe

# 3. در برنامه:
#    - دسته‌بندی انتخاب
#    - سرور انتخاب
#    - "اتصال" کلیک

# 4. تست
#    مرورگر: https://ipinfo.io
```

---

## 📦 فایل نهایی برای توزیع:

### ZIP کردن:

```powershell
cd G:\vpn\desktop-app\dist-package
Compress-Archive -Path VPNApp-win32-x64 -DestinationPath VPNApp-v1.0.0.zip
```

این ZIP (200-250 MB) را به کاربران بدهید:
- دانلود
- استخراج
- اجرای VPNApp.exe

---

**همین! ساده است!** 🚀

برنامه را الان اجرا کردم. چند ثانیه صبر کنید تا پنجره باز شود! 😊
