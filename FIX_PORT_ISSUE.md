# 🔧 رفع مشکل پورت اشغال

## ❌ مشکل:
پورت‌های 10808 و 10809 توسط یک v2ray قدیمی اشغال بودند.

## ✅ راه حل:
Process قبلی را کشتم (PID 20572).

---

## 🚀 برنامه جدید اجرا شد:

**حالا چیکار کنید:**

### 1. پنجره VPNApp را ببینید
- باید دسته‌بندی‌های سرور را ببینید
- **از سرور 89.44.241.83** دریافت می‌شوند

### 2. یک سرور انتخاب کنید

### 3. روی "اتصال" کلیک کنید

### 4. صبر کنید 2-3 ثانیه

### 5. باید ببینید:
- ✅ Alert: "متصل شدید!"
- ✅ پنل سبز می‌شود
- ✅ وضعیت: "متصل"

---

## 🔍 تست اتصال:

### بررسی v2ray:

```powershell
tasklist | findstr v2ray
```

باید یک v2ray.exe جدید ببینید.

### تست IP:

**مرورگر را ببندید و دوباره باز کنید:**
```
https://ipinfo.io
```

باید IP سرور VPN را ببینید!

---

## ⚠️ اگر دوباره خطای Port داد:

یعنی v2ray قبلی هنوز در حال اجرا است.

**راه حل سریع:**

```powershell
# پیدا کردن v2ray
tasklist | findstr v2ray

# کشتن همه
taskkill /F /IM v2ray.exe

# یا با PID:
taskkill /F /PID [NUMBER]

# غیرفعال کردن Proxy
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings" /v ProxyEnable /t REG_DWORD /d 0 /f

# صبر 3 ثانیه
Start-Sleep -Seconds 3

# اجرای مجدد برنامه
.\VPNApp.exe
```

---

## 🎯 نکته مهم:

**همیشه از دکمه "قطع اتصال" استفاده کنید!**

اگر برنامه را ببندید بدون قطع اتصال:
- v2ray همچنان اجرا می‌ماند
- پورت‌ها اشغال می‌مانند
- Proxy فعال می‌ماند

**راه حل:**
- قبل از بستن برنامه، روی "قطع اتصال" کلیک کنید
- بعد برنامه را ببندید

---

**برنامه الان اجرا شده و آماده است!** 🎉

تست کنید و نتیجه را بگویید! 😊

