# راهنمای سریع شروع کار

## برای شروع سریع (5 دقیقه)

### 1️⃣ راه‌اندازی Backend (1 دقیقه)

```bash
cd backend
npm install
npm start
```

✅ باید ببینید: `Server is running on port 3000`

---

### 2️⃣ راه‌اندازی پنل ادمین (1 دقیقه)

**ترمینال جدید باز کنید:**

```bash
cd admin-panel
npm install
npm run dev
```

✅ مرورگر را باز کنید: http://localhost:3001

---

### 3️⃣ ایجاد دسته‌بندی و کانفیگ (2 دقیقه)

در پنل ادمین:

1. روی "دسته‌بندی‌ها" کلیک کنید
2. "+ افزودن دسته‌بندی جدید" را بزنید
3. نام: **ایرانسل**، توضیحات: **سرورهای مخصوص ایرانسل**
4. "ذخیره" را بزنید

5. روی "کانفیگ‌ها" کلیک کنید
6. "+ افزودن کانفیگ جدید" را بزنید
7. وارد کنید:
   - نام: **سرور آلمان 1**
   - لینک کانفیگ: `vless://451785f9-1142-40fb-8943-d615ee3e3737@panel.bananaid.ir:45677?encryption=none&security=none&type=tcp&headerType=http#test-server`
   - دسته‌بندی: **ایرانسل**
   - فعال: ✓
8. "ذخیره" را بزنید

---

### 4️⃣ راه‌اندازی اپلیکیشن موبایل (1 دقیقه)

#### تغییر IP:

فایل `mobile-app/src/api/api.js` را باز کنید و تغییر دهید:

```javascript
// IP کامپیوتر خود را بگذارید
const API_BASE_URL = 'http://192.168.1.100:3000/api';
```

💡 **پیدا کردن IP:**
- Windows: در CMD بنویسید `ipconfig`
- Mac/Linux: در Terminal بنویسید `ifconfig`

#### اجرای اپ:

```bash
cd mobile-app
npm install

# گوشی را به USB وصل کنید و USB Debugging را فعال کنید
npm run android
```

---

## ✨ تمام!

اکنون می‌توانید:
- ✅ در پنل ادمین دسته‌بندی و کانفیگ اضافه کنید
- ✅ در اپلیکیشن موبایل لیست سرورها را ببینید
- ✅ روی سرور کلیک کنید و متصل شوید

---

## 🚨 مشکل دارید؟

### Backend اجرا نمی‌شود
```bash
cd backend
rm -rf node_modules package-lock.json
npm install
npm start
```

### پنل ادمین داده نشان نمی‌دهد
- مطمئن شوید Backend در حال اجرا است (پورت 3000)
- Console را در مرورگر باز کنید (F12) و خطاها را ببینید

### اپلیکیشن موبایل سرورها را نشان نمی‌دهد
- مطمئن شوید IP را درست وارد کرده‌اید
- گوشی و کامپیوتر باید در یک شبکه Wi-Fi باشند
- Backend باید در حال اجرا باشد

### اپلیکیشن Build نمی‌شود
```bash
cd mobile-app/android
./gradlew clean
cd ..
npx react-native start --reset-cache
npm run android
```

---

## 📚 مستندات کامل

برای اطلاعات بیشتر:
- `README.md` - مستندات کامل پروژه
- `INSTRUCTIONS.md` - دستورالعمل مفصل راه‌اندازی
- `backend/README.md` - مستندات API

---

## ⚠️ نکته مهم

این نسخه برای Development است. برای استفاده واقعی (Production):

1. ✅ دیتابیس را به PostgreSQL/MySQL تغییر دهید
2. ✅ Authentication اضافه کنید
3. ✅ از HTTPS استفاده کنید
4. ✅ Native Module برای V2Ray پیاده‌سازی کنید

برای اتصال واقعی به VPN، باید کتابخانه V2Ray Core را به پروژه اضافه کنید.

---

موفق باشید! 🎉

