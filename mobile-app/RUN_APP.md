# 🚀 راهنمای سریع اجرای اپلیکیشن

این راهنما برای کسانی است که می‌خواهند **سریع** اپلیکیشن را اجرا کنند.

## ⚡ روش سریع (5 دقیقه)

### 1️⃣ نصب پکیج‌ها

```bash
cd mobile-app
npm install
```

### 2️⃣ اطمینان از اجرای Backend

```bash
# در ترمینال جداگانه:
cd backend
npm start
```

### 3️⃣ راه‌اندازی Emulator

1. Android Studio را باز کنید
2. کلیک کنید: **Tools > Device Manager**
3. اگر Emulator ندارید: **Create Device**
   - دستگاه: Pixel 5
   - System Image: Android 13 (API 33)
4. دکمه ▶️ Play را بزنید
5. منتظر بمانید تا Emulator باز شود

### 4️⃣ اجرای اپ

```bash
cd mobile-app
npm run android
```

### 5️⃣ صبر کنید!

- اولین بار 2-5 دقیقه طول می‌کشد
- بعد از آن تغییرات سریع اعمال می‌شوند (Hot Reload)

## ✅ موفقیت!

اگر همه چیز درست پیش رفت:
- اپ روی Emulator باز می‌شود
- لیست دسته‌بندی‌ها نمایش داده می‌شود
- می‌توانید روی دسته‌بندی کلیک کنید
- لیست سرورها را ببینید
- به سرور متصل شوید

## 🔧 با گوشی واقعی چطور؟

### تنظیمات گوشی:

1. **Settings** > **About Phone**
2. **7 بار** روی **Build Number** ضربه بزنید
3. برگردید **Settings** > **Developer Options**
4. **USB Debugging** را **ON** کنید

### اتصال:

```bash
# گوشی را با USB وصل کنید
# اجازه Debugging را بدهید
# بررسی کنید:
adb devices

# اگر گوشی را دیدید:
npm run android
```

### تنظیم IP:

فایل `src/api/api.js`:

```javascript
// IP کامپیوتر خود را وارد کنید (ipconfig)
const API_BASE_URL = 'http://192.168.1.X:3000/api';
```

**نکته**: گوشی و کامپیوتر باید در یک شبکه Wi-Fi باشند!

## 🐛 مشکل دارید؟

### خطا: "SDK location not found"

```bash
echo sdk.dir=C:\\Users\\YourUsername\\AppData\\Local\\Android\\Sdk > android/local.properties
```

### خطا: "Unable to load script"

```bash
npm start -- --reset-cache
# در ترمینال دیگر:
npm run android
```

### اپ باز نمی‌شود:

```bash
# حذف و نصب مجدد:
adb uninstall com.vpnapp
npm run android
```

### Backend متصل نمی‌شود:

```bash
# مطمئن شوید Backend در حال اجرا است:
curl http://localhost:3000/api/health

# باید ببینید: {"status":"ok"...}
```

## 📦 چطور APK بگیرم؟

```bash
cd android
gradlew assembleDebug
```

APK در:
```
android/app/build/outputs/apk/debug/app-debug.apk
```

## 🎯 دستورات مفید

```bash
# پاک کردن Cache:
npm start -- --reset-cache

# مشاهده لاگ‌ها:
adb logcat | grep ReactNative

# Reload اپ:
# دو بار R را در ترمینال Metro فشار دهید
# یا: Ctrl+M در Emulator > Reload

# باز کردن Dev Menu:
# Ctrl+M در Emulator
# تکان دادن گوشی
```

## 🎉 تمام!

اگر موفق شدید، تبریک! 🎊

برای اطلاعات بیشتر:
- [SETUP_GUIDE.md](SETUP_GUIDE.md) - راهنمای کامل
- [ANDROID_BUILD.md](ANDROID_BUILD.md) - راهنمای Build
- [README.md](README.md) - مستندات اصلی

موفق باشید! 🚀

