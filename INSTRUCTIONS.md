# دستورالعمل راه‌اندازی سیستم VPN

## پیش‌نیازها

قبل از شروع، نصب موارد زیر الزامی است:

1. **Node.js** (نسخه 18 یا بالاتر)
   - دانلود از: https://nodejs.org/

2. **برای اندروید:**
   - Android Studio
   - Android SDK (API Level 33+)
   - Java Development Kit (JDK 17)

## مرحله 1: راه‌اندازی Backend

```bash
# رفتن به پوشه backend
cd backend

# نصب پکیج‌ها
npm install

# اجرای سرور
npm start
```

سرور روی آدرس `http://localhost:3000` اجرا می‌شود.

### تست Backend:
مرورگر را باز کنید و به آدرس زیر بروید:
```
http://localhost:3000/api/health
```

باید پیام زیر را ببینید:
```json
{
  "status": "ok",
  "message": "VPN API is running"
}
```

## مرحله 2: راه‌اندازی پنل ادمین

**ترمینال جدیدی باز کنید** و دستورات زیر را اجرا کنید:

```bash
# رفتن به پوشه admin-panel
cd admin-panel

# نصب پکیج‌ها
npm install

# اجرای پنل در حالت Development
npm run dev
```

پنل ادمین روی آدرس `http://localhost:3001` اجرا می‌شود.

### استفاده از پنل ادمین:

1. مرورگر را باز کنید و به `http://localhost:3001` بروید
2. ابتدا چند دسته‌بندی ایجاد کنید (مثلاً: "ایرانسل", "همراه اول", "رایتل")
3. سپس کانفیگ‌های VPN را اضافه کنید

#### مثال ایجاد دسته‌بندی:
- نام: "ایرانسل"
- توضیحات: "سرورهای مخصوص ایرانسل"

#### مثال ایجاد کانفیگ:
- نام: "سرور آلمان 1"
- لینک کانفیگ: `vless://451785f9-1142-40fb-8943-d615ee3e3737@panel.bananaid.ir:45677?encryption=none&security=none&type=tcp&headerType=http#germany-server-1`
- دسته‌بندی: "ایرانسل"
- وضعیت: فعال ✓

## مرحله 3: راه‌اندازی اپلیکیشن موبایل

### آماده‌سازی:

1. **تغییر آدرس API:**

فایل `mobile-app/src/api/api.js` را باز کنید و خط زیر را ویرایش کنید:

```javascript
// به جای YOUR_SERVER_IP آدرس IP کامپیوتر خود را بگذارید
const API_BASE_URL = 'http://192.168.1.100:3000/api';
```

برای پیدا کردن IP کامپیوتر:
- **Windows**: در CMD بنویسید: `ipconfig`
- **Mac/Linux**: در Terminal بنویسید: `ifconfig`

2. **نصب پکیج‌ها:**

```bash
cd mobile-app
npm install
```

### اجرای روی اندروید:

#### روش 1: با گوشی واقعی (پیشنهادی)

1. گوشی اندروید را با کابل USB به کامپیوتر وصل کنید
2. در تنظیمات گوشی، "Developer Options" را فعال کنید
3. "USB Debugging" را فعال کنید
4. در ترمینال:

```bash
npm run android
```

#### روش 2: با Emulator

1. Android Studio را باز کنید
2. AVD Manager را باز کنید (Tools > AVD Manager)
3. یک Virtual Device ایجاد کنید
4. Emulator را اجرا کنید
5. در ترمینال:

```bash
npm run android
```

## استفاده از اپلیکیشن

1. اپلیکیشن را باز کنید
2. لیست دسته‌بندی‌ها نمایش داده می‌شود
3. روی یک دسته‌بندی کلیک کنید
4. لیست سرورها نمایش داده می‌شود
5. روی دکمه "اتصال" کلیک کنید

⚠️ **توجه:** در حال حاضر، اتصال VPN واقعی پیاده‌سازی نشده و فقط یک Simulation است. برای اتصال واقعی به سرور VPN باید Native Module برای V2Ray پیاده‌سازی شود.

## مشکلات رایج

### مشکل 1: Backend اجرا نمی‌شود
**راه حل:**
```bash
cd backend
rm -rf node_modules
npm install
npm start
```

### مشکل 2: پنل ادمین به API متصل نمی‌شود
**راه حل:**
- مطمئن شوید Backend در حال اجرا است
- آدرس API را در `admin-panel/src/api/api.js` بررسی کنید

### مشکل 3: اپلیکیشن موبایل سرورها را نمایش نمی‌دهد
**راه حل:**
- مطمئن شوید آدرس IP در `mobile-app/src/api/api.js` صحیح است
- گوشی و کامپیوتر باید در یک شبکه باشند
- Backend باید در حال اجرا باشد

### مشکل 4: اپلیکیشن اندروید Build نمی‌شود
**راه حل:**
```bash
cd mobile-app/android
./gradlew clean
cd ..
npm run android
```

## نکات امنیتی برای Production

قبل از استفاده در Production حتماً موارد زیر را انجام دهید:

1. ✅ Authentication اضافه کنید (JWT Token)
2. ✅ از HTTPS استفاده کنید
3. ✅ دیتابیس را به PostgreSQL یا MySQL تغییر دهید
4. ✅ Input Validation اضافه کنید
5. ✅ Rate Limiting پیاده‌سازی کنید
6. ✅ Logging و Monitoring اضافه کنید

## مراحل بعدی برای پیاده‌سازی VPN واقعی

برای اتصال واقعی به VPN در اندروید:

1. کتابخانه V2Ray Core را به پروژه اضافه کنید
2. Native Module برای React Native بنویسید
3. VPN Service اندروید را پیاده‌سازی کنید
4. Permission های لازم را اضافه کنید

منابع مفید:
- https://github.com/2dust/v2rayNG
- https://github.com/2dust/AndroidLibV2rayLite
- https://developer.android.com/reference/android/net/VpnService

## پشتیبانی

اگر مشکلی داشتید یا سوالی بود، موارد زیر را چک کنید:

1. مطمئن شوید همه پیش‌نیازها نصب شده‌اند
2. مطمئن شوید Backend در حال اجرا است
3. Log های Console را بررسی کنید
4. آدرس IP و Port ها را بررسی کنید

موفق باشید! 🚀

